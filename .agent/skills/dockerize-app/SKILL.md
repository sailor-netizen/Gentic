---
description: Use this to add Docker support.
---
# dockerize-app

## Steps
1. Create `Dockerfile` optimized for multi-stage builds.
2. Create `.dockerignore`.
3. Create `docker-compose.yml` for local dev.
4. Minimize image size (use alpine/distroless).